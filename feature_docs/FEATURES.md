# Feature Index

> **AI Workflow:** Read this file first on every request. Identify the relevant feature, then read only its spec file. Do not read all spec files. When a new feature spec is created, add a row to the table below. There is no separate `architectures/` folder — specs in `feature_docs/` are the single source of truth.
>
> **Spec structure:** Each spec file is a single combined document covering both behavior (entity fields, endpoints, validation, UI) and architecture (Core Values & Principles, Architecture Decisions, Data Flow).
>
> **Status values:** `Draft` → `Spec Complete` → `Scaffolded` → `Implemented` → `Tested` | `Example only` for template references.

| Feature               | Spec File                                       | Data Model                               | Summary                                                                                                                                                                                                                                                                                                       | Status       |
| --------------------- | ----------------------------------------------- | ---------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------ |
| **Feature (Example)** | `feature-spec-feature-example.md`               | —                                        | Template/example entity with a single `Name` field. Used as the canonical reference for generating new features.                                                                                                                                                                                              | Example only |
| **Users**             | `users/feature-spec-users.md`                   | `User`, `UserRole` (enum)                | User accounts with `Email` (unique), `PasswordHash` (nullable for OAuth), `FirstName`, `LastName`, `Role` (`UserRole` enum stored as string), `IsActive`. Admin-only create/list/delete; IDOR protection on read/update (owner or Admin). Only Admin callers may change a user's role.                    | Scaffolded   |
| **Authentication**    | `authentication/feature-spec-authentication.md` | `RefreshToken`, `ExternalLogin` (future) | JWT + refresh token auth. Login with **email**/password; rejects if `IsActive = false` (generic 401). HttpOnly cookie for refresh token, token rotation with reuse detection, silent refresh on app load, return-URL redirect. OAuth-ready: `PasswordHash` nullable, `ExternalLogins` entity documented for future. | Scaffolded   |
| **Room Bookings**     | `room-bookings/feature-spec-room-bookings.md`   | `Location`, `Room`, `Booking`            | Meeting room booking system. Location is a reference table for rooms. Room has name, capacity, location FK, purpose, optional image (JPEG/PNG ≤ 5 MB stored in wwwroot). Booking reserves a room for a time range with overlap prevention. Cascade-delete blocked (room→bookings, location→rooms). All endpoints public. | Scaffolded   |
